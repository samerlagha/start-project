const daysElment = document.querySelector('.week__days');

const daysName =['ПН', 'ВТ', 'СР', 'ЧТ', 'ПТ', 'СБ','ВС'];


//month 

const monthElement = document.querySelector('.header__date');
const monthsName = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];

const setCurrentMonth =()=>{

    const daysNumberElement = document.querySelector('.number')
}